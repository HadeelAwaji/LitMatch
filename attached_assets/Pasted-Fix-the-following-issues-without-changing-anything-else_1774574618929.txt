Fix the following issues without changing anything else in the app:
1. Fix book cover display
Change all book cover images from object-fit: cover to object-fit: contain so the full cover is visible without being cropped. The image container should have a white or very light neutral background so books with transparent covers look clean. Keep the same fixed height of 220px.
2. Fix missing covers
For every book that is currently showing a colored placeholder instead of a real cover, find the correct hardcoded URL using this priority:

Google Books: https://books.google.com/books/content?id=VOLUME_ID&printsec=frontcover&img=1&zoom=3
Open Library: https://covers.openlibrary.org/b/isbn/ISBN-L.jpg
Test every URL before hardcoding it. If no correct cover is found after checking both sources, use a clean solid color placeholder that matches the personality world theme color.

3. Replace inappropriate covers
Check every single book cover in the app. If any cover contains couples kissing, intimate scenes, or any inappropriate imagery, replace that book entirely with a different book that fits the same personality type and has a clean appropriate cover.
4. Fix Arabic app name
When the app is in Arabic mode, change the app name from "ليت ماتش" to "أثر" — keep "LitMatch" in English mode. Update this everywhere the app name appears: navbar, footer, share text, and save result banner.
5. Add Download Result as Image
Using the html2canvas library, add a "Download My Result" button on the results page. When clicked it captures the personality type name, description, and book cards as a clean PNG image that the user can save and share on social media. The image should include the personality world theme colors and the app name "LitMatch" or "أثر" in Arabic mode at the bottom as a watermark. Style the button to match the existing app design.
6. Add Open Graph meta tags
Add these meta tags to the HTML head so when the link is shared on Twitter or WhatsApp it shows the app landing page preview:
html<meta property="og:title" content="LitMatch — Discover Your Reader Personality" />
<meta property="og:description" content="Answer 8 questions and get 5 personalized book recommendations in Arabic and English" />
<meta property="og:image" content="https://litmatch.vercel.app/opengraph.png" />
<meta property="og:url" content="https://litmatch.vercel.app" />
<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:image" content="https://litmatch.vercel.app/opengraph.png" />
Then take a clean screenshot of the landing page and save it as opengraph.png in the client/public folder.
Do not change anything else — no design changes, no question changes, no personality changes, no world theme changes.